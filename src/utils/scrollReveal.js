import ScrollReveal from "scrollreveal";

const sr = ScrollReveal({
  distance: "50px",
  duration: 1000,
  easing: "ease-out",
  origin: "bottom",
  opacity: 0,
  reset: false,
  mobile: true,
});

export default sr;